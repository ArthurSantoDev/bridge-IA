spring:
  application:
    name: bridge-ia
  datasource:
    url: jdbc:h2:mem:bridgeia
    driver-class-name: org.h2.Driver
    jpa:
      hibernate:
        ddl-auto: create-drop

anthropic:
  api-key: ${ANTHROPIC_API_KEY}
  model: claude-sonnet-4-6


